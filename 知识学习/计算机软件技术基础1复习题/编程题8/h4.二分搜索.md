# 题目名称：二分搜索

## 题目描述

编写函数 `binarySearch()`，使用二分搜索，完成在一个有序列表中搜索一个数字。

函数头：

​	`def binarySearch(numbers，number):`

## 输入格式

函数的形参：

​	numbers:一个有序的数字列表，number:被搜素的数字

## 输出格式

函数的返回值：

​	二分搜索过程中，发生数字比较的次数。

## 输入输出样例 #1

### 输入 #1

```
1 2 3 4 5 6
2
```

### 输出 #1

```
3
```

## 输入输出样例 #2

### 输入 #2

```
1 2 3 4 5 6
8
```

### 输出 #2

```
3
```

## 说明/提示

请注意：先将下列模板代码复制，然后在模板的基础上修改完成。只能在`# TODO:`处补充代码，一定不要修改main()函数内部的代码和调用它的语句。

### 模板代码

```python
def binarySearch(a,n): # DO NOT modify this line 
    # TODO: add your code here

# === DO NOT modify the code below ===
def main():
    a = [ eval(x) for x in input().split() ]
    n = eval(input())
    print(binarySearch(a,n))
    
if __name__ == '__main__':
    main()
# === DO NOT modify the code above ===
```

样例组 #1 说明

```
计算公式 mid = (first + last) // 2
第一轮：first=0,last=5,mid=2，比较数字3，2<4，所以last=mid-1=1
第二轮：first=0,last=1,mid=0，比较数字1，2>1，所以first=mid+1=1
第三轮：first=1,last=1,mid=1，比较数字2，2==2，结束。
结论：共比较3次
```

样例组 #2 说明 

```
计算公式 mid = (first + last) // 2 
第一轮：first=0,last=5,mid=2，比较数字3，8>3，所以first=mid+1=3
第二轮：first=3,last=5,mid=4，比较数字5，8>5，所以first=mid+1=5。
第三轮：first=5,last=5,mid=5，比较数字6，8>6，所以first=mid+1=6。
此时，first>last，循环结束，未找到。
结论：共比较3次
```