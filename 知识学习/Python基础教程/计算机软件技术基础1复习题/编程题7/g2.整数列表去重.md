# 题目名称：整数列表去重

## 题目背景

1.  本次作业是完成一个函数的编写，请按照题目要求的功能，严格遵守题目中对函数头、形参和返回值的规定完成。
1.  只可以在  `# TODO: ` 的地方加上你的代码，模板其它部分必须原样照抄，不可修改！
1.  所有的输入输出都只能出现在 main() 函数中，其它地方一律不要使用 print() 和 input()，也就是说，在补充 TODO 处代码时，不可以执行任何 print() 和 input() !

## 题目描述

编写一个函数`eliminateDuplicates()`，输入一个整数列表，返回此列表的一个复本，重复的数字只保留第一个，复本中的数据要保留与原列表相同的顺序。在函数中不要修改输入的原列表的值。

函数头

​	`def eliminateDuplicates(numbers):`

## 输入格式

函数的形参：

​	numbers，一个整数列表

## 输出格式

函数的返回值：

​	一个整数列表，内容为numbers的复本，其中重复的数字仅保留第一个。

## 输入输出样例 #1

### 输入 #1

```
eliminateDuplicates(map(int,"5 8 2 9 8 2 8".split()))
```

### 输出 #1

```
5 8 2 9
```

## 输入输出样例 #2

### 输入 #2

```
eliminateDuplicates(map(int,"321 58 892 321".split()))
```

### 输出 #2

```
321 58 892
```

## 说明/提示

**请注意**：先将下列模板代码复制，然后在模板的基础上修改完成。只能在`# TODO:`处补充代码，一定不要修改main()函数内部的代码和调用它的语句，即不要修改以下二行注释中间的语句。
```
# === DO NOT modify the code below ===
不要修改这二行注释中间的语句！
# === DO NOT modify the code above ===
```
### 模板代码

```python
def eliminateDuplicates(numbers): # DO NOT modify this line 
    # TODO: add your code here

# === DO NOT modify the code below ===
def main():
    list1 = eval(input())
    print(" ".join(map(str,list1)))
    
if __name__ == "__main__":
    main()
# === DO NOT modify the code above ===
```