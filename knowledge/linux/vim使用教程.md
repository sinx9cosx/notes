---
tags:
  - computer
Category:
  - 笔记
---
# 模式

`vim`打开，默认命令模式。

![[vim.png]]

打开/创建文件：

``` linux
vi hello.txt
```

默认进入命令模式

# 命令

## 尾行模式

`:q`退出

`:wq`保存并退出

## 插入模式

命令模式下，通过i/I，a/A，o/O进入插入模式

i：光标前面插入 I：行首插入

a：光标后面插入 A：行尾插入

o：当前行的下一行 O：当前行的上一行

## 命令模式

`^`行首

`$`行尾

`yy`复制

``