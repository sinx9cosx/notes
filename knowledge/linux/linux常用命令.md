---
tags:
  - computer
  - 计算化学
  - research
Category:
  - 笔记
---
`ls`查看当前目录下的所有文件和目录

`ls -l`显示更详细的信息

`ls-a`可以显示包括隐藏文件（.开头的文件）在内的所有文件和目录

第一个字符表示文件类型

`-`表示普通文件

`l`表示链接文件（硬链接和软链接，软链接类似快捷方式）

`d`表示目录

`ln`创建链接文件（默认硬链接） `ln -s+文件名称+被指向文件的链接`表示创建一个软链接文件

