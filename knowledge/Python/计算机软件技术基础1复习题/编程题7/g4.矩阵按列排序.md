# 题目名称：矩阵按列排序

## 题目背景

1.  本次作业是完成一个函数的编写，请按照题目要求的功能，严格遵守题目中对函数头、形参和返回值的规定完成。
1.  只可以在  `# TODO: ` 的地方加上你的代码，模板其它部分必须原样照抄，不可修改！
1.  所有的输入输出都只能出现在 main() 函数中，其它地方一律不要使用 print() 和 input()，也就是说，在补充 TODO 处代码时，不可以执行任何 print() 和 input() !

## 题目描述

编写一个函数 `sortColumns()` ，将一个二维列表（为一个矩阵，即每行中的元素数量相同，但注意不一定是方阵，即行数不一定等于列数）按列排序。函数返回一个新列表而原始列表保持不变。

函数头：

​	`def sortColumns(matrix, reverse=False):`

## 输入格式

函数的形参：

​	matrix，一个二维列表。

 reverse，是否逆序，reverse等于False代表升序，等于True代表降序，默认为False（升序）。

## 输出格式

函数的返回值：

​	按列排序后的新列表。

## 输入输出样例 #1

### 输入 #1

```
3
1 2 3 4
3 2 1 -3
2 3 2 -2
sortColumns(mx1)
```

### 输出 #1

```
1 2 1 -3
2 2 2 -2
3 3 3 4
```

## 说明/提示

**请注意**：先将下列模板代码复制，然后在模板的基础上修改完成。只能在`# TODO:`处补充代码，一定不要修改main()函数内部的代码和调用它的语句，即不要修改以下二行注释中间的语句。
```
# === DO NOT modify the code below ===
不要修改这二行注释中间的语句！
# === DO NOT modify the code above ===
```
### 模板代码

```python
def sortColumns(matrix, reverse=False): # DO NOT modify this line 
    # TODO: add your code here

# === DO NOT modify the code below === 
def main():
    mx1 = []
    n = int(input())
    for i in range(n):
        numbers = [ eval(x) for x in input().split() ]
        mx1.append(numbers)
    mx2 = eval(input())
    for row in mx2:
        print(" ".join(map(str,row)))
    
if __name__ == "__main__":
    main()
# === DO NOT modify the code above ===
```