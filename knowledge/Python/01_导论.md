

## Python简介

### 什么是Python

- Python是一门解释型、面向对象的高级编程语言。
- Python是开源免费的、支持交互式、可跨平台移植的脚本语言。

### Python 历史

- Python 于20世纪90年代初，由荷兰人吉多·范罗苏姆 （Guido van Rossum）基于C语言开发。
- 之所以选中 Python（大蟒蛇的意思）作为该编程语言的名字，是因为 Guido 是室内情景喜剧《蒙提.派森的飞行马戏团》（Monty Python's Flying Circus）的忠实观众。
- 目前使用的 Python 版本主要有 Pyton 2.x 和 Python 3.x 两种，而后者并不完全兼容前者的语法。如果没有特殊的应用需求，建议使用 Python 3.x 。
- 官方网站：https://www.python.org

### Python 的特点

- 优点：简单易学、免费开源、跨平台性、高层语言、面向对象、代码库丰富、胶水语言……
- 缺点：运行速度慢、代码不能加密……

扩展知识：编译型语言 vs 解释型语言

- 编译型语言：将源代码在指定平台上一次性编译为目标代码，之后可以多次执行目标代码。语法错误可以在编译期被发现，每次修改后都需要重新编译，但执行速度较快。典型代表：C、C++、Java、C#等。
- 解释型语言：源代码每次运行，都需要逐行解释然后执行。 语法错误在执行时被发现，每次修改后不需要重新编译，但执行速度较慢。典型代表：Python、JavaScript等。

### Python应用领域

- 主要应用领域：系统编程、图形界面开发、科学计算、文本处理、数据库编程、网络编程、Web开发、自动化运维、金融分析、多媒体应用、游戏开发、人工智能、网络爬虫……
- 数据分析与数据可视化：NumPy、SciPy、Pandas、Matplotlib等可以让 Python 程序员编写科学计算程序，对数据进行清洗、去重、规格化和针对性的分析，从而完成科学计算、数据分析和数据可视化。
- 人工智能：Python 在人工智能领域内的机器学习、神经网络、深度学习等方面都是主流的程序语言，得到广泛的支持和应用。
- 网络应用：大数据行业获取数据的核心工具是网络爬虫，对于各种网络协议的支持很完善，Python 是编写网络爬虫和其它网络应用软件的主流编程语言， Scrapy 爬虫框架应用非常广泛。
- Web开发：Python 有 Flask、Django、TurboGears、web2py 等优秀框架，方便开发各种 Web 应用。
- 自动化运维：Python 可以方便的处理文本数据、CSV文件、Excel表格、JSON格式数据等各种类型的数据，也可以连接 SQLite、MySQL 等数据库管理数据。Python 脚本可以分析日志、安排定时任务及发送电子邮件。
- 桌面软件：使用 PyQt、PySide、wxPython、PyGTK 等可以快速开发基于 Python 的桌面应用程序。
- 游戏：Python 有 Pygame 等编写游戏的模块。很多游戏使用 C++ 编写图形显示等高性能模块，而使用 Python 编写游戏的逻辑引擎服务器。

## 本地安装 Python

### 安装Python运行环境（必装）

**请注意在Python官网上下载Python运行环境，此软件是完全免费的！不要被误导下载一些其他垃圾软件，更不要花钱购买！！！**

到 https://www.python.org/ 下载 python运行环境，Windows平台下载的文件类似于：python-3.12.2-amd64.exe

在本地运行 python-3.12.2-amd64.exe，（或者下载其他版本的python环境）,注意红框里面两个对号都打上。

![image-20250916194640739](image-20250916194640739.png)



点击【Install Now】。然后再点击【close】结束安装。

按下键盘上的 Win + R 键，打开“运行”对话框。输入 cmd 并按 Enter 键，打开命令提示符。输入 python 如果出现类似下图则表示python环境安装成功：

![image-20250916194723561](image-20250916194723561.png)



关于环境的安装也可以参考 https://www.w3cschool.cn/python3/python3-install.html



## 本地运行 Python 

**下面截图以python3.13为例子截的，如果用其他版本，则截图略有不同** 

Python安装完后，在开始菜单会有如下的一个文件夹：

![image-20250916195229664](image-20250916195229664.png)

分别是：

- **IDLE**： 可以用来解释执行Python代码，也可以用来写Python代码。

- **Python**：Python的解释器，可以用来解释执行Python代码，使用命令行调用Python命令的时候就是运行的这个程序。

- **Python Manuals**：这个是Python的使用手册，可以用来学习Python的基础使用。

- **Python Module Docs**：这个是Python的模块文档，可以用来学习模块的使用。

**请运行工具 IDLE 。IDLE作为Python官方自带的开发调试学习工具，可以满足我们后续课程的需求。**

---

### Python代码执行方式  

Python代码的执行方式有两种，一种是 **交互型** 的解释执行方式，另一种是将Python写成 **代码** 的形式，然后使用解释器执行，两种执行方式有所不同：

C/C++源文件名扩展名一般为 .h .c .cpp ,编译以后可执行文件是 .exe; Python程序源文件一般扩展名为 .py

Python 文件**不需要显式编译**。Python 是一种解释型语言，它的代码在运行时由 Python 解释器逐行解释执行，而不是像 C 或 C++ 那样需要先编译成机器码。

---

以下是 Python 代码执行的基本过程：

### 1. **解释执行**

- Python 代码以 `.py` 文件的形式保存。
- 当你运行 Python 文件时（例如通过命令 `python script.py`），Python 解释器会逐行读取代码并执行。
- 解释器会将代码转换为一种中间形式，称为**字节码（Bytecode）**，然后由 Python 虚拟机（PVM）执行这些字节码。

### 2. **字节码生成**

- Python 解释器在运行代码时，会先将代码编译成字节码（`.pyc` 文件），这是一种优化机制。
- 字节码文件通常保存在 `__pycache__` 目录中，文件扩展名为 `.pyc`。
- 字节码的生成是自动的，用户不需要手动干预。


### 与编译型语言的区别

- **编译型语言**（如 C、C++）：代码需要先编译成机器码，生成可执行文件，然后才能运行。
- **解释型语言**（如 Python）：代码由解释器逐行解释执行，不需要显式编译。
- 虽然 Python 不需要显式编译，但在某些情况下，你可以手动生成字节码文件

---


### 执行方式一：交互型执行方式

这种执行方式类似人与人之间的对话，写一句代码执行一句代码。这种类型类似人的交互行为，所以被称为交互执行方式，使用这种交互方式最大的特点是：在命令行界面中有`>>>`作为每行代码的开头。如下图所示：

![python解释器](https://atts.w3cschool.cn/attachments/image/20211012/1634006505683208.jpeg)

 **接下来本手册中的代码只要是带有`>>>`的都是在 交互型 解释运行后的结果，没有`>>>`的代码都是Python代码（有时候又叫脚本代码）。** 

进入交互型解释器的方式有如下几种方式：

- 命令行方式：按`Win + R`键，输入`cmd`，点击回车，进入命令行界面，然后输入`python`，点击回车，即可进入命令行界面，在命令行中输入`python`并按回车执行，即可进入命令行界面。
- 运行方式：按`Win + R`键，输入`python`，点击回车，即可直接进入Python解释器页面。
- 直接运行程序：安装的Python中里面有一个选项是`Python`，直接点击这个选项就可以进入Python解释器。
- IDLE：安装的Python中里面有一个选项是`IDLE`，直接点击后进入的也是Python解释器。

![IDLE shell界面](https://atts.w3cschool.cn/attachments/image/20211012/1634007125782650.jpeg)

#### 一个例子：

```python
>>>print("hello")
```

#### 如何退出？

在交互型解释器中输入`exit()`按回车即可退出。

![退出](https://atts.w3cschool.cn/attachments/image/20211012/1634007386960944.gif)




> 在交互型解释器中，代码如果会产生输出，会直接打印在控制台，输出和代码的区别是：代码前面有`>>>`。

### 执行方式二：代码执行方式

这种方式是大多数编程语言执行方式（先写代码，然后一次执行），**这种方式也是未来我们学习Python编程中最常遇到的编程方式**  。

### 使用 IDLE 进行代码执行

在开始菜单中搜索 "IDLE" 并点击打开，或者 按下键盘上的 Win + R 键，打开“运行”对话框。输入 IDLE并按 Enter 键

点击打开IDLE，我们可以进入到Python的shell（命令行，也就是交互型解释器）界面，点击`File`，选择`New File`。可以新建一个代码文件（也可以使用`Ctrl + N`进行新建代码文件）。就会弹出一个窗口，这时候我们就可以在这个窗口里面写Python代码了。

> 虽然IDLE相对比较简单，但它也提供了代码着色等实用的功能，如果没有什么很好的Python代码编辑器，不妨尝试一下它！

写完代码后可点击`run`，选择`run module`（或者直接按`F5`），就可以运行代码了。

![IDLE运行代码](https://atts.w3cschool.cn/attachments/image/20211012/1634008698932697.gif)

> 初次运行需要先保存代码并给代码文件命名，后续代码更改也要重新保存代码。

另外，IDLE也可以打开以`.py`为后缀的文件，与新建类似，点击`File`，选择`Open...`，可以打开本地的Python代码文件（py代码文件以`.py`为后缀）。运行方式与新建方式相同。

> 这意味着Python可以使用其他的代码编辑器进行代码编写，再使用IDLE去运行，你甚至可以用记事本去编程。

目前最主流的Python编程环境，还有 PyCharm 和 VSCode 等。


### 如何使用命令行运行Python代码？

在命令提示符中使用`python`命令就可以运行Python代码了，`python`命令的使用方法如下：

```
python 要运行的文件.py
```

示例：

![命令行运行Python代码](https://atts.w3cschool.cn/attachments/image/20211012/1634009862673029.jpeg)

#### Q&A：什么是命令行？

命令行是系统提供的一种工具，可以用这些工具来执行一些系统提供的功能，常见的命令行有：Linux的终端，Windows的cmd，Windows的PowerShell（进阶版的cmd）。

命令行在一些工具中也存在，比如说`vscode`就提供了命令行窗口，Python著名的IDE`pycharm`也有提供命令行窗口和交互型解释器的窗口。

在Windows操作系统 输入 win+R 然后输入 cmd 回车即可进入命令行模式。

## 在线运行

- **Onlinegdb** : https://www.onlinegdb.com/ 

注意：【Language】选【Python 3】

使用起来非常简单，但是速度较慢，功能比别的在线网站丰富，但是有时候无法访问

- https://www.w3schools.com/python/python_compiler.asp

点击【Try it yourself】使用起来非常简单，但是速度较慢，功能简陋，个别时候无法访问

- https://www.w3cschool.cn/python3/python3-comment.html

这个网站很多章节例子代码下面都有【尝试一下】按钮，点开尝试按钮就可以使用，使用起来非常简单，但是速度较慢，功能简陋，访问较稳定

- 如果想用海龟做图的话，这个也可以 https://turtle.codemao.cn/ 或者是这个网址 https://wood.codemao.cn/


---

一个简单的hello world 代码

```python
print("Hello, World!")
#这里是注释
print("2^10=",2**10)
print("2^100=",2**100)
#python中变量使用前不需要声明
a=1
b=10
c=a+b
print(c)
print(a,b,c)
```


运行结果如下

```
Hello, World!
2^10= 1024
2^100= 1267650600228229401496703205376
11
1 10 11
```



